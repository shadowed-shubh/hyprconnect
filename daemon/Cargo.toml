[package]
name = "daemon"
version = "0.1.0"
edition = "2024"

[dependencies]
hyprconnect-core = { path = "../core" }

tokio = { version = "1", features = [
    "rt-multi-thread",
    "macros",
    "signal",
] }

tracing = "0.1"
tracing-subscriber = "0.3"
anyhow = "1"
